# Configuration Files

This directory contains project configuration files:

- `go.mod` - Go module definition
- `.gitignore` - Git ignore patterns

These files are separated from the main project structure to keep the root directory clean and focused on documentation and source code.